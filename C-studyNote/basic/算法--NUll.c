算法：
    通俗定义：
        解题的方法和步骤
    狭义的算法：
        对存储数据的操作
        对不同的存储结果，要完成某一个功能所执行的操作是不一样的
        比如：要输出数组中所有的元素的操作和要输出链表中所有元素的操作是不一样的
        这说明：
        算法是依附于存储结构的，不同的存储结构，所执行的算法是不一样的
    广义的算法：
        广义的算法也叫泛型
        无论数据是如何存储的，对该数据的操作都是一样的
【null的含义】：
二进制全部为0的含义--0000000的含义
    1>表示数值零
    2>表示的是字符结束标识符‘\n’
    3>空指针NUll
        NULL本质也是零，而这个零不代表数字零，而表示的是内存单元的标号零
 
    计算机规定了，以零为编号的存储单元的内容不可读，不可写
